import React from 'react';

const Support = () => {
  return (
    <div>
      <h1 style={{ marginBottom: '1rem' }}>Поддержка</h1>
      <p style={{ color: 'var(--text-dim)' }}>Handle support tickets and queries.</p>
    </div>
  );
};

export default Support;
